api:
  security:
    token:
      secret: ${JWT_SECRET:chave_secreta_local_treinodex_123456789}

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/treinodex_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true